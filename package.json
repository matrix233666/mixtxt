{
  "name": "mixtxt",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "check": "astro check",
    "test": "vitest --run --passWithNoTests",
    "validate:content": "node -e \"process.exit(0)\"",
    "build": "npm run validate:content && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.10.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@types/node": "^22.15.30",
    "typescript": "^5.8.3",
    "vitest": "^2.1.8"
  }
}
